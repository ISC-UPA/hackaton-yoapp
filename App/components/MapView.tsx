import React from 'react';
import { StyleSheet } from 'react-native';
import MapView from 'react-native-maps';

const NativeMapView = (props: any) => {
  return <MapView {...props} />;
};

export default NativeMapView;